TIME
this.props.time

SUMMARYCONTAINER
this.props.summaryContainer.map(x =>
<span>x</span>
)

BREAKDOWNCONTAINER
this.props.breakdownContainer.map(x => {
  <span>x.type</span>
  <span>x.percent</span>
  <span>x.total</span>
})

PLANTBREAKDOWN
inputVal = default || onClick
this.props.plantBreakdown.map(x => 
  x.**"inputVal"**.ASSET
  x.**"inputVal"**.TNG
  x.**"inputVal"**.MC
)